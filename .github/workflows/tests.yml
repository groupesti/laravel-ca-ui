name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php: ['8.4']
        laravel: ['12.*']
        stability: [prefer-stable]

    name: PHP ${{ matrix.php }} - Laravel ${{ matrix.laravel }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: dom, curl, libxml, mbstring, zip, pdo, sqlite, pdo_sqlite, openssl
          coverage: xdebug

      - name: Configure Satis repository
        run: |
          composer config repositories.groupesti composer https://package-app.com
          composer config http-basic.package-app.com "${{ secrets.SATIS_USER }}" "${{ secrets.SATIS_PASSWORD }}"

      - name: Install dependencies
        run: |
          composer require "laravel/framework:${{ matrix.laravel }}" --no-interaction --no-update
          composer update --${{ matrix.stability }} --no-interaction

      - name: Run tests
        run: ./vendor/bin/pest --coverage --min=80
